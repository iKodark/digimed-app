import { TranslateRolesPipe } from './translate-roles.pipe';

describe('TranslateRolesPipe', () => {
  it('create an instance', () => {
    const pipe = new TranslateRolesPipe();
    expect(pipe).toBeTruthy();
  });
});
