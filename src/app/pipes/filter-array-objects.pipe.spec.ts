import { FilterArrayObjectsPipe } from './filter-array-objects.pipe';

describe('FilterArrayObjectsPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterArrayObjectsPipe();
    expect(pipe).toBeTruthy();
  });
});
